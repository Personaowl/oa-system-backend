<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.personaowl.oa.ai.infrastructure.mapper.AiIndexTaskMapper">

    <resultMap id="BaseResultMap" type="com.personaowl.oa.ai.domain.entity.AiIndexTask">
        <id column="id" property="id"/>
        <result column="task_no" property="taskNo"/>
        <result column="doc_id" property="docId"/>
        <result column="task_type" property="taskType"/>
        <result column="status" property="status"/>
        <result column="error_message" property="errorMessage"/>
        <result column="retry_count" property="retryCount"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.personaowl.oa.ai.domain.entity.AiIndexTask" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_index_task
        (task_no, doc_id, task_type, status, error_message, retry_count, created_by, created_at, updated_at)
        VALUES
        (#{taskNo}, #{docId}, #{taskType}, #{status}, #{errorMessage}, #{retryCount}, #{createdBy}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="updateById" parameterType="com.personaowl.oa.ai.domain.entity.AiIndexTask">
        UPDATE ai_index_task
        SET task_no = #{taskNo},
            doc_id = #{docId},
            task_type = #{taskType},
            status = #{status},
            error_message = #{errorMessage},
            retry_count = #{retryCount},
            created_by = #{createdBy},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM ai_index_task WHERE id = #{id}
    </select>

    <select id="selectPage" resultMap="BaseResultMap">
        SELECT *
        FROM ai_index_task
        WHERE 1 = 1
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="taskType != null and taskType != ''">
            AND task_type = #{taskType}
        </if>
        <if test="docId != null">
            AND doc_id = #{docId}
        </if>
        ORDER BY created_at DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

</mapper>
