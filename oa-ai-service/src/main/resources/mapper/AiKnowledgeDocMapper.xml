<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.personaowl.oa.ai.infrastructure.mapper.AiKnowledgeDocMapper">

    <resultMap id="BaseResultMap" type="com.personaowl.oa.ai.domain.entity.AiKnowledgeDoc">
        <id column="id" property="id"/>
        <result column="doc_title" property="docTitle"/>
        <result column="doc_domain" property="docDomain"/>
        <result column="doc_version" property="docVersion"/>
        <result column="file_name" property="fileName"/>
        <result column="file_url" property="fileUrl"/>
        <result column="content_hash" property="contentHash"/>
        <result column="status" property="status"/>
        <result column="source_type" property="sourceType"/>
        <result column="effective_date" property="effectiveDate"/>
        <result column="approved_by" property="approvedBy"/>
        <result column="approved_at" property="approvedAt"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_by" property="updatedBy"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted" property="deleted"/>
        <result column="version" property="version"/>
    </resultMap>

    <insert id="insert" parameterType="com.personaowl.oa.ai.domain.entity.AiKnowledgeDoc" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_knowledge_doc
        (doc_title, doc_domain, doc_version, file_name, file_url, content_hash, status, source_type, effective_date,
         approved_by, approved_at, created_by, created_at, updated_by, updated_at, deleted, version)
        VALUES
        (#{docTitle}, #{docDomain}, #{docVersion}, #{fileName}, #{fileUrl}, #{contentHash}, #{status}, #{sourceType}, #{effectiveDate},
         #{approvedBy}, #{approvedAt}, #{createdBy}, #{createdAt}, #{updatedBy}, #{updatedAt}, #{deleted}, #{version})
    </insert>

    <update id="updateById" parameterType="com.personaowl.oa.ai.domain.entity.AiKnowledgeDoc">
        UPDATE ai_knowledge_doc
        SET doc_title = #{docTitle},
            doc_domain = #{docDomain},
            doc_version = #{docVersion},
            file_name = #{fileName},
            file_url = #{fileUrl},
            content_hash = #{contentHash},
            status = #{status},
            source_type = #{sourceType},
            effective_date = #{effectiveDate},
            approved_by = #{approvedBy},
            approved_at = #{approvedAt},
            updated_by = #{updatedBy},
            updated_at = #{updatedAt},
            deleted = #{deleted},
            version = #{version}
        WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM ai_knowledge_doc WHERE id = #{id}
    </select>

    <select id="selectPage" resultMap="BaseResultMap">
        SELECT *
        FROM ai_knowledge_doc
        WHERE deleted = 0
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="docDomain != null and docDomain != ''">
            AND doc_domain = #{docDomain}
        </if>
        <if test="keyword != null and keyword != ''">
            AND (doc_title LIKE CONCAT('%', #{keyword}, '%')
                 OR doc_version LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY created_at DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

</mapper>
